class Solution {
    public String solution(int n) {
        String answer = "";
        // answer = n + "";
        // answer = Integer.toString(n);
        answer = String.valueOf(n);
        return answer;
    }
}